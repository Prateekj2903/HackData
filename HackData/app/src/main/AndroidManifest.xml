<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pri.android.hackdata">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/vidya_logo"
        android:roundIcon="@mipmap/vidya_logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SpeachQuestionActivity">

        </activity>
        <activity android:name=".SpeakQuestionActivity">

        </activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AnswerAcceptActivity">

        </activity>
        <activity android:name=".AnswerRejectActivity">

        </activity>


        <service
            android:name=".ReceiveMessageService"
            android:enabled="true"
            android:exported="true"></service>
    </application>

</manifest>